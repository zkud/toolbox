#!/bin/bash

readonly VIM_IMAGE_NAME="toolbox-vim"
readonly VIM_USER_NAME=zkud
readonly VIM_GROUP_NAME=chads
readonly VIM_USER_ID=$(id -u) # Current User's ID
readonly VIM_GROUP_ID=$(id -g) # Current User's Group ID
